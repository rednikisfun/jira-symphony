CRITICAL RULE: The `grapesjs_editor/` directory is DEPRECATED and DEAD CODE. Do NOT read, search, grep, or modify ANY file inside `grapesjs_editor/`. Ignore it completely as if it does not exist. The active page/content editor is `turbolink_editor/` (TypeScript/Vite). All editor-related work MUST happen in `turbolink_editor/`.

---

You are implementing Jira issue {{ issue.key }}: "{{ issue.summary }}"

## Issue Details
- **Type:** {{ issue.issue_type }}
- **Priority:** {{ issue.priority }}
{% if project %}
- **Project:** {{ project.key }} — {{ project.description }}
{% endif %}

## Description
{{ issue.description or "No description provided." }}
{% if issue.comments %}

## Comments
{% for comment in issue.comments %}
- {{ comment }}
{% endfor %}
{% endif %}
{% if project and project.key == "analytics" %}

## Analytics API Reference
The backend API documentation is available as a Postman collection at:
`/Users/isfun/Dev/Next.js/backend-service-analytics-laravel/postman_collection.json`

Read this file to find the correct API routes, request/response formats, and available endpoints.

If you need an API route that does NOT exist in the Postman collection and cannot implement the feature without it, you MUST:
1. State clearly in your summary which route is missing and what it should do
2. Add the label `missing-api-route` to the issue by including this exact line at the END of your summary: `[LABEL:missing-api-route]`
{% endif %}

## Instructions
1. Read CLAUDE.md and AGENTS.md in this repository for conventions.
2. If Bug → find root cause first. If Task/Story → plan the approach.
3. Implement following the project's architecture and coding conventions.
4. Run linters/formatters/tests as described in CLAUDE.md.
5. Commit with conventional commits referencing the issue key
   (e.g., `fix: resolve i18n bug ({{ issue.key }})`).
6. Do NOT push or create merge requests — the orchestrator handles that.
7. Output a brief summary of what you changed and why, in Russian.
